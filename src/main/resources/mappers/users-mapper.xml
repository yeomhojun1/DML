<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="users">
	<insert id="joinMember">
		insert into users(user_id, user_pwd,user_enabled, authorities) 
    	values (#{userId}, #{userPwd}, default, #{authorities})
	</insert>

	<select id="idCheck" resultType="string">
		select user_id
		from users
		where user_id = #{userId}
	</select>
	
	<select id="readMember" resultType="FpUsersVo">
		select *
		from users
		where user_id = #{userId}
	</select>

	<select id="readMemberWithIDPW" resultType="FpUsersVo">
		select *
		from users 
		where user_id=#{userId} and user_pwd=#{userPwd}
	</select>
	
</mapper>
