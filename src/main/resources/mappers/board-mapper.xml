<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="board">
	<select id="selectList" resultType="FpBoardVo">
		select board_no , member_id,Board_title,board_content, board_date,board_Count, bstep, bleveL from board order by board_No desc
	</select>
	

	<select id="selectOne" resultType="FpBoardVo"
		parameterType="_int">
		select * from board where board_no=#{boardNo}
	</select>

	<insert id="insert" parameterType="FpBoardVo">
		insert into board (board_no, member_id,Board_title,board_content,board_date,board_count) values (board_no_seq.NEXTVAL,#{memberId},#{boardTitle},#{boardContent},sysdate,0)
	</insert>
	<!-- insert into board (board_no, member_id, Board_title,board_content,board_date) 
		values(board_no_seq.NEXTVAL, #{memberId},#{boardTitle},#{boardContent},#{boardDate},0 
		, , , ) -->
	<update id="update" parameterType="FpBoardVo">
		update board set member_Id = #{memberId},board_Title =
		#{boardTitle},board_Content = #{boardContent},
		board_Date = sysdate
		where board_no = #{boardNo}
	</update>
	<update id="plusCount" parameterType="FpBoardVo">
		update board set board_Count = #{boardCount}+1
		where board_no = #{boardNo}
	</update>
	<delete id="delete" parameterType="_int">
		delete from board where board_No = #{boardNo}
	</delete>
</mapper>
