<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="board">
	<select id="selectList" resultType="FpBoardVo">
		select board_no , member_id,Board_title,board_content,TO_CHAR(board_date,'YYYY-MM-DD'),board_Count, bref, bstep, bleveL from board order by board_No desc
	</select>
	

	<select id="selectOne" resultType="FpBoardVo"
		parameterType="_int">
		select * from where board_no=#{boardNo}
	</select>

	<insert id="insert" parameterType="FpBoardVo">
		insert into board (board_no, member_id,
		Board_title,board_content,board_date)values(board_no_seq.NEXTVAL,
		#{memberId},#{boardTitle},#{boardContent},#{boardDate})
	</insert>
	<!-- insert into board (board_no, member_id, Board_title,board_content,board_date) 
		values(board_no_seq.NEXTVAL, #{memberId},#{boardTitle},#{boardContent},#{boardDate},0 
		, , , ) -->
	<update id="update" parameterType="FpBoardVo">
		update board set memberId = #{memberId},boardTitle =
		#{boardTitle},boardContent = #{boardContent},
		boardCount = #{boardCount},boardDate = #{boardDate}
		where commentNum = #{commentNum}
	</update>

	<delete id="delete" parameterType="_int">
		delete from board where commentNum = #{boardNo}
	</delete>
</mapper>
