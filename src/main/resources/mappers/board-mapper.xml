<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="board">
  <select id="selectList" resultType="FpBoardVo">
		select * from board
	</select>
	
	<select id="selectOne" resultType="FpBoardVo" parameterType="_int">
	select * from  where commentNum=#{commentNum}
	</select>
	
	<insert id="insert" parameterType="FpBoardVo">
	insert into board values(#{commentNum}, #{memberId},#{boardTitle},#{boardContent},#{boardCount},#{boardDate})
	</insert>
	
	<update id="update" parameterType="FpBoardVo">
	update board set memberId = #{memberId},boardTitle = #{boardTitle},boardContent = #{boardContent},
	boardCount = #{boardCount},boardDate = #{boardDate}
	where commentNum = #{commentNum}
	</update>
	
	<delete id="delete" parameterType="_int">
	delete from board where commentNum = #{commentNum}
	</delete>
</mapper>
